// Módulo sin dependencias (no toca Prisma ni AWS SDK) para poder importarlo
// tanto desde el backend como desde componentes cliente del frontend.
export const MAX_LESSON_VIDEO_BYTES = 50 * 1024 * 1024; // 50 MB
